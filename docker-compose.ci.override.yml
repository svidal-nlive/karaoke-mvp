services:
  watcher:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  metadata:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  splitter:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  packager:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  organizer:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  telegram_youtube_bot:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  status-api:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

  maintenance:
    command: ["pytest", "."]
    environment:
      - TEST_MODE=1

networks:
  default:
    name: karaoke-mvp_default
