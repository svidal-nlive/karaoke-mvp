# Cloudflare Tunnel Token (optional, for public access)
TUNNEL_TOKEN=replace-with-your-token

# ---- Core Pipeline ----
REDIS_HOST=redis
REDIS_PORT=6379

# ---- Telegram Bot ----
TELEGRAM_BOT_TOKEN=replace-with-your-telegram-bot-token
TELEGRAM_CHAT_ID=replace-with-your-chat-id

# ---- Optional: Slack / Email Notifications ----
SLACK_WEBHOOK_URL=
NOTIFY_EMAILS=
SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# ---- Maintenance/Cleanup ----
CLEANUP_CRON_SCHEDULE=0 3 * * *   # Daily at 3AM; override as needed

# ---- Directories (rarely change) ----
INPUT_DIR=/input
QUEUE_DIR=/queue
META_DIR=/metadata/json
STEMS_DIR=/stems
OUTPUT_DIR=/output
ORG_DIR=/organized
LOGS_DIR=/logs

# ---- For YouTube/Deemix Downloader Services (optional) ----
YT_DLP_OPTS=
DEEMIX_USER=
DEEMIX_PASS=
